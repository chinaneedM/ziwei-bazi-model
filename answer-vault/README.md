# 公共仓库加密答案区

本系统不需要私人答案仓库。旧合成开发例使用
`encrypted/<DEV-EXAMPLE-ID>.json.fernet`；正式107例只允许通过原子批量导入生成
`formal/<CASE_ID>.json.fernet`及不含答案的哈希清单。两类密文均可随公共仓库发布。
明文答案和密钥不得进入仓库。

正式评分前，107例必须由一个仓库外的完整可信答案批次一次性校验并加密。
缺一例、重复一例、题号不全、选项无效或密钥不匹配都会关闭激活门。评分器只在预测
冻结后解密；仓库中不保存逐题明文答案。把明文答案直接公开会破坏未揭盲条件，
控制器会拒绝这种做法。
