# 安全与答案隔离

明文答案和 `FORTUNE_ANSWER_KEY` 绝不能提交到 Git、日志、训练状态或知识修正中。

- 明文答案文件必须放在仓库根目录之外。
- `encrypt-answer` 拒绝读取仓库内的明文答案。
- `score` 只有在预测已冻结后才解密答案。
- 仓库内只记录正确数、题数、准确率和是否达标；逐题正确答案只允许写到仓库外的临时复盘文件。
- 怀疑密钥或答案泄漏时，应立即轮换密钥、重新生成全部加密答案，并废弃受影响轮次。

安全问题请通过仓库维护者提供的私密渠道报告，不要在公开 Issue 中粘贴答案或密钥。
