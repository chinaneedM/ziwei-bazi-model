# DEV-GROUP-002 source reimport intent

This branch contains only staging fragments and a one-time deterministic materializer until GitHub Actions verifies and commits the canonical runtime objects. It does not change astrological knowledge, question semantics, ranking rules, or case-direction rules. DEV-GROUP-001 history remains untouched.
