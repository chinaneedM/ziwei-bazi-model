# Group signal format

Create one JSON file per new group under this directory. The file name is the group run identifier. The body contains a schema, status, group identifier, session identifier, and mode. Repository workflows validate and process the file.
