name: research-shiqie-bh-finalize

on:
  push:
    branches: [agent/fusion-chart-core-r1-20260822]
    paths:
      - .github/workflows/research-shiqie-bh-finalize.yml

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Finalize Batch 12BH research artifacts
        run: |
          python .github/scripts/finalize-shiqie-bh.py
          python scripts/verify-fusion-chart-historical-provenance-audit-r1.py
          python scripts/verify-project-continuity-state-r1.py
          git diff --check
      - name: Commit Batch 12BH
        run: |
          rm -f .github/scripts/finalize-shiqie-bh.py
          rm -f .github/workflows/research-shiqie-rishu-public-payload-probe.yml
          rm -f .github/workflows/research-shiqie-bh-finalize.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs scripts .github
          git status --short
          git commit -m "close Batch 12BH Shiqie transcription anomaly"
          git push origin HEAD:agent/fusion-chart-core-r1-20260822
