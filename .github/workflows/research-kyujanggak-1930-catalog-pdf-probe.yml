name: research-kyujanggak-1930-catalog-pdf-probe

on:
  workflow_dispatch:
  push:
    branches:
      - agent/fusion-chart-core-r1-20260822
    paths:
      - ".github/workflows/research-kyujanggak-1930-catalog-pdf-probe.yml"
      - "scripts/research_kyujanggak_1930_catalog_pdf_probe.py"

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install isolated research dependency
        run: python -m pip install --disable-pip-version-check curl_cffi
      - name: Probe official M/F PDF list
        run: |
          python scripts/research_kyujanggak_1930_catalog_pdf_probe.py \
            --output artifacts/kyujanggak-1930-catalog-pdf-probe
      - name: Upload PDF route evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kyujanggak-1930-catalog-pdf-probe-${{ github.sha }}
          path: artifacts/kyujanggak-1930-catalog-pdf-probe/
          if-no-files-found: warn
          retention-days: 7
