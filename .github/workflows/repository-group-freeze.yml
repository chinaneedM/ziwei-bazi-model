name: repository-group-freeze

on:
  push:
    paths:
      - 'runtime/group-freeze-requests/*.json'
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'runtime/group-freeze-requests/*.json'
      - 'data/group-clean-starts/*/postblind-predictions/*.json'
      - 'src/fortune_v1/end_to_end.py'
      - 'scripts/group-training-pipeline.py'
      - 'tests/test_end_to_end_pipeline.py'
      - '.github/workflows/repository-group-freeze.yml'
  pull_request_target:
    types: [opened, synchronize, reopened]
    paths:
      - 'runtime/group-freeze-requests/*.json'
  workflow_dispatch:
    inputs:
      request_path:
        description: 'Exact runtime/group-freeze-requests/*.json path'
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: repository-group-freeze-${{ github.event.pull_request.head.ref || github.ref }}
  cancel-in-progress: false

jobs:
  freeze-group:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout exact request branch
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install runtime
        run: python -m pip install -e .

      - name: Resolve exact freeze requests
        shell: bash
        env:
          MANUAL_REQUEST_PATH: ${{ inputs.request_path }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          : > /tmp/group-freeze-requests.txt
          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            request="${MANUAL_REQUEST_PATH}"
            case "$request" in
              runtime/group-freeze-requests/*.json) ;;
              *) echo "invalid manual request path: $request" >&2; exit 1 ;;
            esac
            test -f "$request"
            printf '%s\n' "$request" > /tmp/group-freeze-requests.txt
          elif [[ "${GITHUB_EVENT_NAME}" == "pull_request" || "${GITHUB_EVENT_NAME}" == "pull_request_target" ]]; then
            git diff --name-only "${PR_BASE_SHA}...${PR_HEAD_SHA}" -- 'runtime/group-freeze-requests/*.json' \
              | sort -u > /tmp/group-freeze-requests.txt
            test -s /tmp/group-freeze-requests.txt
          else
            git diff-tree --no-commit-id --name-only -r "$GITHUB_SHA" -- 'runtime/group-freeze-requests/*.json' \
              | sort -u > /tmp/group-freeze-requests.txt
            test -s /tmp/group-freeze-requests.txt
          fi
          while IFS= read -r request; do
            case "$request" in
              runtime/group-freeze-requests/*.json) ;;
              *) echo "invalid request path: $request" >&2; exit 1 ;;
            esac
            test -f "$request"
          done < /tmp/group-freeze-requests.txt

      - name: Run freeze gate tests
        run: python -m unittest tests.test_end_to_end_pipeline -v

      - name: Validate predictions and freeze group
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/group-freeze-results
          while IFS= read -r request; do
            request_name="$(basename "$request" .json)"
            PYTHONPATH=src python scripts/group-training-pipeline.py freeze-group \
              --request "$request" \
              | tee "/tmp/group-freeze-results/${request_name}.json"
          done < /tmp/group-freeze-requests.txt

      - name: Verify group freeze receipts
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path

          results = sorted(Path('/tmp/group-freeze-results').glob('*.json'))
          if not results:
              raise SystemExit('no group freeze result')
          for path in results:
              result = json.loads(path.read_text(encoding='utf-8'))
              if result.get('status') != 'GROUP_PREDICTION_FREEZE_PASS':
                  raise SystemExit(f'group freeze failed: {result}')
              if result.get('answer_data_available') is not False:
                  raise SystemExit('answer data became available before group freeze')
              if result.get('all_predictions_frozen_before_reveal') is not True:
                  raise SystemExit('freeze order gate failed')
              if result.get('case_count') != len(result.get('cases', [])):
                  raise SystemExit('case freeze count mismatch')
              if result.get('question_count', 0) <= 0:
                  raise SystemExit('question freeze count invalid')
              for row in result.get('cases', []):
                  receipt = Path(row['freeze_path'])
                  if not receipt.is_file():
                      raise SystemExit(f'missing case freeze: {receipt}')
                  body = json.loads(receipt.read_text(encoding='utf-8'))
                  if body.get('status') != 'CASE_PREDICTION_FREEZE_PASS':
                      raise SystemExit(f'invalid case freeze: {receipt}')
                  if body.get('frozen_before_reveal') is not True:
                      raise SystemExit(f'case freeze order failed: {receipt}')
          PY

      - name: Commit immutable prediction freezes
        shell: bash
        env:
          TARGET_BRANCH: ${{ github.event.pull_request.head.ref || github.ref_name }}
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add data/group-clean-starts
          if git diff --cached --quiet; then
            echo 'No prediction freeze objects to commit.'
            exit 0
          fi
          git commit -m 'Freeze group predictions before reveal'
          git push origin "HEAD:${TARGET_BRANCH}"
