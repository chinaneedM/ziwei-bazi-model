# Fusion Chart Historical Provenance Audit R1 — Batch 12DQ

## 大統 C-II-N 內部整刻層 × 南京59刻端點制度層：固定舍入閾值反證與復合重構

Status: **SINGLE GLOBAL CONSTANT FRACTIONAL THRESHOLD DISPROVED FOR C-II-N INTERIOR + 59 ENDPOINT / PRE-1578 OFFICIAL NANJING 59-KE ENDPOINT REUSED AS AN INDEPENDENT INSTITUTIONAL ANCHOR / C-II-N 42–58 INTERIOR THRESHOLD LAYER + NANJING 59 ENDPOINT NOW FORM A MECHANICALLY SUFFICIENT COMPOSITE VALUE-ARCHITECTURE CANDIDATE / EXACT PRE-1578 TEXT BINDING BOTH LAYERS STILL UNRESOLVED / SANMING-YUELING DAHAN-YUSHUI FINGERPRINT STILL UNRESOLVED / ZERO EXACT-PARENT VOTE / NO RUNTIME CHANGE / NO ALGORITHM REOPEN**

## 1. Why this batch

12DP closed seventeen consecutive direct C-II-N integer crossings from 42 through 58 and aligned the later 1624 clean stepped family to the historical qi-ce coordinate. It also left one deliberate contradiction:

```text
C-II-N summer endpoint = 58.6332 ke
received Nanjing-family endpoint = 59/41
```

The next gate asked for a pre-1578 Nanjing/Datong reduction or selection rule that can explain the endpoint without silently promoting a modern rounding convention.

This batch does two narrower things:

1. tests and rejects the entire class of **one constant fractional round-up threshold** as a global C-II-N -> clean 42–59 mapping;
2. reuses already direct pre-1578 official evidence to model 59 as a separate Nanjing institutional/locality endpoint layer rather than forcing it through the interior reduction rule.

It does **not** claim that a pre-1578 text has yet been found which explicitly writes the combined procedure.

## 2. Evidence firewall

No new physical-glyph claim is created here. The batch composes already audited evidence:

- 12DP: direct no-OCR C-II-N 42–58 adjacent integer crossings and the 58.6332-ke summer endpoint;
- 12CF: 1447 official Ming record directly prints Nanjing solstitial extreme 59 ke; 41 is only the independently established hundred-ke complement in that memorial;
- 12CL: 1449 official restoration order returns future calendar production to the Hongwu/Yongle old style;
- 12CP: the exact 1578 Sanming physical target directly prints summer-solstice 59/41 and the anomalous Dahan/Yushui change-day phrases;
- `EXT-XINGYUNLU-GUJIN-LVLIKAO-V47-NANJING-59-41`: later Ming explanatory control only. It explains early-Ming Datong as recalibrated to the Nanjing clepsydra 59/41 standard, but it postdates 1578 and contributes zero ancestry vote.

Chronology remains separated throughout.

## 3. A single constant fractional threshold is mathematically impossible

Test the restricted rule class:

```text
Q_t(x) = floor(x) + 1, if frac(x) >= t
         floor(x),     otherwise

one constant t for the whole curve
```

12DP directly fixed the final interior 58-ke crossing:

```text
d160 = 57.9862 ke   (lower side)
d161 = 58.0424 ke   (first crossing of 58)
```

For the clean stepped-family replay to keep d160 on 57 before the first 58 crossing:

```text
t > 0.9862
```

But the summer endpoint is:

```text
d182 = 58.6332 ke
received endpoint = 59
```

To turn 58.6332 into 59 under the same threshold rule:

```text
t <= 0.6332
```

Therefore:

```text
required interior interval: t > 0.9862
required endpoint interval: t <= 0.6332
intersection: EMPTY
```

This is stronger than merely saying “floor fails at summer solstice.” It rejects the whole **single constant fractional threshold** family for simultaneously reproducing the 12DP interior replay and the 59 endpoint.

Common special cases fail in complementary ways:

| candidate | d160 = 57.9862 | summer = 58.6332 | result |
| --- | ---: | ---: | --- |
| floor | 57 | 58 | interior representative fits; endpoint fails |
| nearest integer | 58 | 59 | endpoint fits; interior lower-side transition is too early |
| ceiling | 58 (and d161 -> 59) | 59 | interior crossing architecture fails |

This is a model-class negative result only. It does not identify the historical positive rule.

## 4. The endpoint already has an independent pre-1578 institutional anchor

12CF directly closed a 1447 official Nanjing standard:

```text
Nanjing summer-solstice day = 59 ke
Nanjing winter-solstice night = 59 ke
```

The same memorial states that palace/government clepsydra arrows were Nanjing old style. 12CL then closed the 1449 official restoration to the Hongwu/Yongle old style after the Beijing-adjusted regime.

Accordingly, the summer 59 does not have to be generated by a universal fractional threshold applied to the C-II-N endpoint. There is already a historically independent pre-1578 **regional/institutional endpoint standard** available as a separate component.

The later Xing Yunlu explanation is useful only as a postdated semantic control: it explicitly associates early-Ming Datong day/night calibration with the Nanjing clepsydra 59/41 branch. It is not promoted into a Sanming ancestor.

## 5. Composite mechanism candidate

The mechanically sufficient value architecture is now:

```text
C-II-N continuous Nanjing/Datong daily curve
  -> direct interior whole-ke crossings 42..58
+
1447 official Nanjing solstitial endpoint
  -> 59 ke (41 as hundred-ke complement in that memorial)
+
1449 restoration of Hongwu/Yongle old style
  -> policy continuity context
------------------------------------------------
composite Nanjing value architecture 42..59
```

This resolves the 12DP **mechanical endpoint contradiction** without inventing one global rounder.

What remains unresolved is more specific and more historical:

```text
Which pre-1578 Chinese text explicitly binds those two layers?
Which source gives the exact Sanming/Yueling Dahan 十三後 and Yushui 後四日 change days?
Was the endpoint implemented as cap, table lookup, clepsydra-arrow selection, term-specific recomposition, or another rule?
```

No answer is invented.

## 6. Tianwen transmission graph impact

New rule-family hypothesis node:

```text
RULE-FAMILY-NANJING-CII-INTEGER-THRESHOLD-PLUS-59-ENDPOINT-ANCHOR
```

New edges:

```text
TG-E0057  C-II-N daily table
          --SYNTHESIS_COMPONENT_CANDIDATE_FOR-->
          composite rule-family node

TG-E0058  1447 Nanjing 59-ke standard
          --SYNTHESIS_COMPONENT_CANDIDATE_FOR-->
          composite rule-family node

TG-E0059  composite rule-family node
          --STRUCTURAL_MECHANISM_CANDIDATE_FOR-->
          post-1578 clean Nanjing 59/41 stepped family
```

The graph also records an explicit unresolved non-edge from the composite node to direct ancestry of the 1578 Sanming table. The new node is a research reconstruction candidate, not a newly discovered historical text.

## 7. Product adjudication

```text
MATRIX_ROWS=198
AUDITED_ROWS=166
CURRENT_MISSING_FROM_PRODUCT=10
HPA-ZDATE-006=MISSING_FROM_PRODUCT
CONFIRMED_PROVENANCE_METADATA_DEFECT_COUNT=12
REPAIRED_PROVENANCE_METADATA_DEFECT_COUNT=12
CONFIRMED_CHART_ALGORITHM_DEFECT_COUNT=0
ALGORITHM_REOPEN_COUNT=0
CANDIDATE_COLLAPSE_COUNT=0
DETERMINISTIC_FUSION_CHART_PRODUCT_R1=CLOSED
```

There is no new runtime candidate, no winner, no candidate collapse and no chart-algorithm reopen.

## 8. Next gate

The search is now narrower:

1. find a **pre-1578 Chinese physical or securely dated textual witness** that explicitly binds the Nanjing/Datong daily whole-ke selection layer to the 59-ke endpoint standard;
2. prioritize Tongshu/almanac, clepsydra/change-arrow, official-calendar and Nanjing/Jiangnan witnesses that also preserve the exact Sanming/Yueling `大寒十三後 / 雨水後四日` fingerprint;
3. continue an independent Chinese physical-carrier route for C-II-N / 晨昏立成;
4. keep NLC call 14202 parallel behind the external-action firewall; do not submit identity/payment/authorization-bearing requests without user action.

Research record: `docs/research/ZIWEI-DATONG-NANJING59-ENDPOINT-RECOMPOSITION-R1.json`.
