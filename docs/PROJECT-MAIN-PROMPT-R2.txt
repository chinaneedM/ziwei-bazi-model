PROJECT_INSTRUCTION_ID=FORTUNE-GENERALIZATION-TRAINING-R2
PROJECT_INSTRUCTION_STATUS=RETIRED
SUPERSEDED_BY=docs/PROJECT-BOOTSTRAP-R1.txt

# Retired control-plane tombstone

This file is retained only so old links fail safely and audibly. It is not an active
project prompt, runtime policy, training policy, model rule, maintenance schedule, or
source authority.

Do not copy dynamic training thresholds, current state, model-learning rules, maintenance
cadence, current round identifiers, or source-version details from this historical identity.
The active project layer is the minimal Git-only Bootstrap R1 audit copy. All mutable
runtime behavior is resolved from the current `main` branch.
