# R1训练政策一致性与知识批次A审计

## 审计基线

- 基线提交：`d65f8e0c56cd2664cccdd253ac6caad7ee91c163`
- 题库：107例、511题、0阻塞
- 例题98：输入已修复
- 例题29：来源暴露的开发参考，不计严格首次盲测
- 正式答案信封：0/107
- 数据集状态：`DATASET_FROZEN_AWAITING_ANSWER_IMPORT`
- 运行状态：`PAUSED_NO_NEW_PREDICTIONS`

本轮未运行案例预测、未读取或导入答案、未激活安全启动包、未修改`sources/canonical/`。

## 政策冲突与修复

| 冲突位置 | 基线行为 | R1修复 |
|---|---|---|
| `config/training-policy.json` | `ONE_SCORED_FIRST_BLIND_ROUND` | 改为同案连续3轮门槛；失败清零并留案 |
| `runtime.start_round` | 已有首次盲测即禁止再开轮 | 第一轮标记`FIRST_BLIND`，后续轮标记`SAME_CASE_REPLAY` |
| `runtime.score_round` | 单轮通过立即推进 | 连续次数达到3才推进；失败归零 |
| `runtime.apply_learning` | 失败修正规则后推进下一案 | 只发布`model-learning`修正并回到同一案例 |
| `chat_input` | 安全包宣告单轮单案 | 输出当前连续次数、目标3次和同案重试规则 |
| `issue_relay` | 结果固定为首次盲测并返回下一案 | 返回真实轮次类型、连续次数和是否仍需同案轮 |
| `verify` | 验证一次性首次盲测状态机 | 验证R1连续门槛、历史迁移例外和同案证据隔离 |
| 文档与项目指令 | 明示废止连续3轮 | 统一恢复R1，题级V3仅作诊断和证据治理层 |

保留的V3能力：题级标签与错误诊断、首次盲测统计、三分区、复合原子、规则证据账本和未来不同案例验证。`SAME_CASE_REPLAY`只计入R1案例门，不计首次盲测，也不产生独立跨案例规则证据。

现有合成控制器的`DEV-EXAMPLE-001`在R1恢复前已被旧政策标记完成，因此`training/state.json`明确登记唯一的历史迁移例外；未来完成案例必须满足连续3轮门。

## 批次A：S04–S07派生知识

- 16张批次A静态知识卡：十二宫、人物入口、实体所有权、复合原子、对宫三方、主星本性、条件修饰、格局成立、破格制化、十二基础盘、六十星系、空宫借星、结构选择器、入宫蕴含、能力上限和父链反证。
- 10项冲突矩阵：明确宫位坐标、人物太极、单星与星系、格局成立与破格、结构匹配和现代终点之间的优先关系。
- 16项案例覆盖映射：只读取107例/511题的无答案`preblind_profile`，不读取结果；覆盖不等于验证。
- 7张原有方法总纲卡迁移到知识卡Schema V2。

全部23张卡包含精确来源行号、流派归属、前置输入、执行步骤、适用与否定条件、异文冲突、反例、同源去重键、能力上限、禁止越级点、题目标签与验证状态。全部保持`CURATED_UNVALIDATED`，支持数和反例数均为0；来源命例固定为`USAGE_DEMONSTRATION_ONLY_NOT_INDEPENDENT_VALIDATION`。

## 用户经验与AI推论治理

`research-hypothesis.schema.json`和`research-hypotheses.json`把两类来源限定为：

- `USER_HYPOTHESIS`
- `AI_HYPOTHESIS`

两者使用相同验证门槛，不因提出者身份增加权重，也不能直接升级为真理。每条假设必须登记基础发生率计划、选择偏差、确认偏误控制、地域年代、替代解释、反事实、负例计划、答案争议、置信度与校准方案。

## 验证结果

- 运行完整性：通过；107例、511题、0阻塞、正式答案0/107。
- 来源哈希：通过；20个冻结来源与`canonical-manifest`一致，S04–S07前后哈希逐文件一致。
- 答案隔离：通过；案例库`answer_payload_present=false`，正式答案信封0/107。
- 案例分区：通过；开发65、阶段验证21、最终保留21，无同人跨区泄漏。
- 知识Schema：通过；23张卡、10项冲突、16项覆盖映射。
- 自动化测试：27项通过。
- 训练状态：继续暂停；`prediction_allowed=false`。
