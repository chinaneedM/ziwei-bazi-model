# Fusion Chart Historical Provenance Audit R1 — Batch 10A

## Bazi raw stem/branch relations and neutral affinity/exposure facts

Status: **AUDITED / PHILOLOGICAL RELATION DECOMPOSITION / ONE NEW SOURCE-CLOSED PRODUCT GAP / NO ALGORITHM REOPEN**

## 1. Neutral affinity is not root-strength doctrine

The repository's `affinity` label is a software normalization. Its mechanics are limited to:

- exact visible-stem == hidden-stem identity;
- same-five-element visible-stem/hidden-stem identity.

These are deterministic projections from already audited hidden-stem membership and stem identities. They do not mean 通根、得地、强根、主根 or any strength grade.

`HiddenStemExposureLink` is likewise exact identity only.

## 2. Included raw relation families

The current runtime geometries are source-supported:

- five heavenly-stem combinations;
- six branch harmonies;
- six branch clashes;
- the six 相穿 pairs;
- four standard three-combination bureaus;
- directed punishment cycles, Zi-Mao mutual punishment and repeated self-punishment.

Occurrence is kept separate from successful transformation, strength, good/bad outcome or event prediction.

## 3. 穿 / 害 terminology

Frozen YHZP uses `相穿` and its commentary says `相穿为害`. Ming received texts commonly call the same six pairings `六害`.

This is treated as a philological name bridge over one mechanical pair geometry. Runtime correctly emits one `CHUAN` fact rather than double-counting both CHUAN and HARM.

## 4. Punishment labels

The directed pair geometry agrees across received witnesses, but the frozen YHZP transcription and 《三命通会》 swap the labels `无恩` and `恃势` for the 寅巳申 and 丑戌未 cycles.

Runtime encodes the directed geometry but not those disputed category names, so there is no algorithm defect.

## 5. New source-closed product gap: 辰戌丑未土局

S14's frozen YHZP chapter 8 includes `辰戌丑未土局`; Ming 《三命通会》 independently uses the same four-earth-bureau expression.

Current runtime emits only the four standard three-member trines. The four-earth grouping is therefore `MISSING_FROM_PRODUCT` as a distinct arity-4 candidate.

It must not be forced into `BRANCH_TRINE`, because that would corrupt both the historical wording and the data-model arity.

## 6. Accounting

```text
TOTAL_MATRIX_ROWS=181
TOTAL_AUDITED_ROWS=148
CONFIRMED_CHART_ALGORITHM_DEFECT_COUNT=0
ALGORITHM_REOPEN_COUNT=0
IDENTIFIED_MISSING_CANDIDATE_FAMILY_COUNT=9
```
