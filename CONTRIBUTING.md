# 贡献规则

本仓库只接受能提高**通用推理能力**的修正。

- 不得写入某案例、某问题、某选项的正确答案或固定映射。
- 不得为了通过现有 5 个案例加入“看到某描述就选 A/B/C/D”的规则。
- 知识修正必须写明适用条件、边界、反例或能力上限，以及影响的 S00–S19 库编号。
- 修改训练政策时必须同步更新测试；不得绕过“冻结后评分”和“连续 3 轮达标”规则。
- 新案例输入与答案必须物理分离；答案只以加密形式保存。

提交前运行：

```bash
make verify
make test
```
